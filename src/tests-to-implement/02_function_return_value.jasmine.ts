describe('function mock return value', () => {
  describe('getAllItemsOnSale', () => {
    xit('returns only prices under 10', async () => {
      // Arrange
      // Act
      // Assert
    })
  })
})
